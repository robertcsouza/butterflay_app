class User {
  String? name, email, password, thumbnail;

  User({String? name, String? email, String? password, String? thumbnail}) {
    this.name = name;
    this.email = email;
    this.password = password;
    this.thumbnail = thumbnail;
  }
}
